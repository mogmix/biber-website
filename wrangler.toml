name = "biber-website"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[vars]
# Non-secret vars go here. Secrets (DATABASE_URL, AUTH_TOKEN) are set
# via the Cloudflare Pages dashboard or `wrangler pages secret put`.
